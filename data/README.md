# Data

- `cache/paper_data_de2000_jab.npz` is the compact artifact used by `python main.py --mode npz ...`.

Full recomputation does not read private measurements or local lab files. It loads standard illuminants, reflectance datasets, and color-matching functions from LuxPy.
